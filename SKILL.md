---
name: mingli-tuiyan
description: 八字命理四视角推演（陆致极《命运的求索》方法论的开源算法版）——强弱评分/调候表/格局初判/五行形象，纯通用算法、不含任何个人信息，任何人可输入自己的出生信息使用。
category: consultation
---

# 命理推演 · 四视角法（开源通用版）

结合陆致极《命运的求索——中国命理学简史及推演方法》的多视角推演框架，
把"解盘"中可复现的部分做成算法：**强弱（扶抑）→ 调候（穷通宝鉴）→ 格局（月令取格）→ 形象（五行）**。
排盘 + 推演全程不内置任何个人命盘数据，纯参数输入。

## 启用条件

用户提到：推演八字、看强弱/调候/格局、用陆致极方法、喜用神判断、四视角分析。

## 使用流程

```bash
# 1) 自动排盘并四视角推演（示例日期为虚构占位，请替换为实际出生时间）
python scripts/tuiyan.py --solar 1990-06-15 --time 10:30 --gender 男

# 2) 已有四柱直接推演
python scripts/tuiyan.py --bazi "庚午 壬午 辛亥 癸巳" --gender 男

# 3) 只排盘（四柱/十神/藏干/大运）
python scripts/paipan.py --solar 1990-06-15 --time 10:30 --gender 男
```

运行环境：需要 Python 3 + lunar-python（`pip install -r requirements.txt`）

## 四视角算法说明

| 视角 | 算法 | 输出 |
|------|------|------|
| ① 强弱 | 得令(月令旺相休囚死 ±3/2/-1/-2/-3) + 得地(禄位+3、同五行藏干根 2/1/0.5、印根×0.8) + 得势(天干印比 ±、财官食伤 -) | 总分 → 身强/偏强/中和/偏弱/身弱 + 喜用方向 |
| ② 调候 | 查 `tiaohou_table.py`（穷通宝鉴 10干×12月）→ 对照命局已现/未现 | 当月主用神清单 |
| ③ 格局 | 月支本气十神 → 正格八格初判 + 透干参考 | 初判格局名 |
| ④ 形象 | 干支+藏干五行计数 → 最旺/最弱/缺 | 五行分布数据 |

权重常量集中在 `tuiyan.py` 顶部（W_MONTH/ROOT_SCORE/±分值），可调。

## 铁律

1. **通用性**——本技能是开源通用版，代码和数据文件**不得写入任何个人命盘/个人信息**（含分析对象真实盘）。个人数据留在别处，需要时以参数喂入。
2. **纯算模式**——脚本只输出可复现的算法初判；**综合解读（气象/性情/大运应期/六亲/合盘）不自动生成**，需分析师完成并标注依据。
3. **口径透明**——排盘按输入时间直排；真太阳时需使用者自行换算；调候表注明"通行本整理、版本有差异"。
4. **D级红线**——命理一律作概率性倾向参考，不作决策依据（关系/投资/健康判断不进决策层）。
5. **可证伪**——涉及应期的解读要登记"预测+验证条件"，到期验证，不做无法证伪的空预测。

## 文件

```
scripts/paipan.py         通用排盘（四柱/十神/藏干/大运）
scripts/tuiyan.py         四视角推演主脚本（强弱+调候+格局+形象）
scripts/tiaohou_table.py  穷通宝鉴调候表（独立数据文件，可校对修改）
```
